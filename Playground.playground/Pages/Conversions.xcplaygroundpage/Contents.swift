/*:
 ### Converts from a type to another
 > See Boolean, UInt, Int, Float, Double, Decimal.
 */

import SwiftXtend


let bool = Double(2).to.bool

let decimal = Double(2.5).to.decimal

let index = UInt(3).to.indexPath


//: < [Summary](Summary) | [Next](@next) >
