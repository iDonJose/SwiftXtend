import SwiftXtend

/*:
 ## `adding(rows:sections:)`
 Adds to rows and sections
 > See also `removing(rows:sections:)`
 */

let index = IndexPath(row: 10, section: 2)
let newIndex = index.adding(rows: 3, sections: 1)


//: < [Summary](Summary) | [Next](@next) >
